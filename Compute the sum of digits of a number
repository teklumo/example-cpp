#include <iostream>
using namespace std;
int main(){
    int n,sum(0);
    cout<<"enter the number";
    cin>>n;
    while(n>0){
    int num=n%10;
    sum=sum+num;
    n=n/10;
    }
    cout<<"the sum of the digits is:"<<sum;
    return 0;
    }
